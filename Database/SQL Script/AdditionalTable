
use TailorManagement;
GO


EXEC sp_rename 'Product.Image', 'ImageFront', 'COLUMN';



-- Add a new column
ALTER TABLE Product
ADD ImageBack VARCHAR(255);


Create table Ties (
	TiesID int PRIMARY key,
	size DECIMAL(10, 2),
	style NVARCHAR(100)
);

Alter table Ties 
 add CONSTRAINT FK_T_Pt
		FOREIGN KEY (TiesID) REFERENCES Product (ProductID)
		ON DELETE CASCADE
        ON UPDATE CASCADE;